If you only intend to use the library, all you need is the TiledScreen.zip file in the libraries folder.

It is sufficient to import the library into the Arduino IDE directly as a .zip file.

The file also contains an example of use.

